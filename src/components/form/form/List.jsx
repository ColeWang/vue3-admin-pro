import { defineComponent } from 'vue'

// @todo 列表支持
export default defineComponent({
    inheritAttrs: false,
    setup () {
        return () => {
            return (
                <div></div>
            )
        }
    }
})
