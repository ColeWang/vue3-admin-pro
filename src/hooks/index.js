import { createAppInstance, useAppInstance } from './useAppInstance'
import useLocaleMessage from './useLocaleMessage'
import useThemeConfig from './useThemeConfig'

export {
    createAppInstance,
    useAppInstance,
    useLocaleMessage,
    useThemeConfig
}
